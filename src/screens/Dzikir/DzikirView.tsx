import * as React from 'react';
import {View} from 'react-native';
import {Dzikir} from '../../types';

type DzikirViewProps = {
  dzikir: Dzikir;
};

const DzikirView = (props: DzikirViewProps) => {
  return <View />;
};
export default DzikirView;
